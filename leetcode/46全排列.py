class Solution:
    def permute(self, nums: list):
        n = len(nums)
        new_list = []
        for num in nums:
            print(num)
            new_list.append(num)
            print(new_list)

        # for i in range(len(nums)):
        #     if
        # return nums

a = Solution().permute([1,2,3])
print(a)